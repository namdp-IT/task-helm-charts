replicaCount: 2
image:
  repository: ghcr.io/namdp-it/task-backend
  tag: "v1.0.0"
  pullPolicy: Always
service:
  port: 8080
  targetPort: 8080
secretNames:
  - postgres-secret
env:
  - name: DB_PORT
    value: "5432"
